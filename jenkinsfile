pipeline{
    agent any
    stages{
        stage('verifier la version de newman'){
        agent{
            docker{
                image "postman/newman"
                args '--entrypoint=""'
            }
        }
            steps{
                sh "newman --version" // test
                   }
        }
    }
}

