pipeline {
    agent any
    stages {
        stage('Test API') {
            agent {
                docker {
                    image 'postman/newman'
                    args '--entrypoint=""'
                }
            }
            steps {
                sh 'newman run collections/collection1.json'
            }
        }
    }
}