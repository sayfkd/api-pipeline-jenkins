pipeline {
    agent any
    stages {
        stage('Test API') {
            agent {
                docker {
                    image 'postman/newman'
                    //args '--entrypoint=""'  // Si nécessaire
                }
            }
            steps {
                sh 'newman run collections/collection1.json'
            }
        }
    }
}